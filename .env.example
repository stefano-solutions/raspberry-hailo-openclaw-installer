# Sample environment configuration for install-openclaw-rpi5.sh

# Optional: run installer in offline mode (true/false)
OFFLINE_MODE=false

# Optional: prepare offline bundle (true/false)
PREPARE_OFFLINE=false

# Optional: use sanitizing proxy for hailo-ollama (true/false)
USE_SANITIZER_PROXY_ON_OLLAMA=true

# Optional: enable OpenClaw tools (true/false)
USE_OPENCLAW_TOOLS=true

# Optional: select assistant flavor (openclaw | picoclaw | zeroclaw | nanobot | moltis | ironclaw | nullclaw)
CLAW_FLAVOR=openclaw

# Optional: select model ID (must be Hailo-Ollama compatible)
# HAILO_MODEL=qwen2:1.5b

# -----------------------------------------------------------------------------
# hailo-sanitize-proxy.py flags
# -----------------------------------------------------------------------------
# These are read by hailo-sanitize-proxy.py.
# If you run proxy via systemd, add them to the service environment.

# Comma-separated allowed browser origins for CORS.
HAILO_PROXY_ALLOWED_ORIGINS=http://localhost:8787,http://127.0.0.1:8787

# Allow requests with no Origin header (CLI/local services). Set to 0 to require Origin.
HAILO_PROXY_ALLOW_NO_ORIGIN=1

# Comma-separated allowed Host header values.
HAILO_PROXY_ALLOWED_HOSTS=127.0.0.1:8081,localhost:8081,[::1]:8081,127.0.0.1,localhost,[::1]

# Allowed request headers for CORS preflight responses.
HAILO_PROXY_CORS_ALLOW_HEADERS=Authorization,Content-Type,X-Requested-With

# Proxy trace controls.
HAILO_PROXY_TRACE=1
HAILO_PROXY_TRACE_DIR=/tmp/hailo-proxy-traces
HAILO_PROXY_TRACE_MAX_BYTES=250000

# Sanitizer caps to keep Hailo requests lightweight.
HAILO_PROXY_MAX_TOKENS=128
HAILO_PROXY_MAX_HISTORY_MESSAGES=1
